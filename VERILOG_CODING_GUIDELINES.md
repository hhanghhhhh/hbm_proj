# Verilog 编码约定（Codex）

本约定限制 RTL 的基本形式、工程风格和模块顶部行为契约的写法，不规定具体实现方式。Codex 可根据模块复杂度选择合适设计，但应保持以下风格一致。

## 1. 基本编码约束

- 生成可综合、可验证、可维护的 Verilog-2001 代码；除非明确要求，不使用 SystemVerilog 专有语法。
- 命名使用 `snake_case`。输入、输出端口分别使用 `i_`、`o_` 前缀；低有效信号使用 `_n` 后缀。端口名应能体现信号用途和数据流向。时钟使用 `i_clk`，复位使用 `i_rst_n`。
- 参数、局部常量和状态常量使用大写命名；状态常量使用 `ST_` 前缀。模块例化使用具名参数和具名端口连接。
- 状态机默认使用一段式写法，在同一个时序 `always` 块中完成状态转移和寄存输出；当逻辑明显更适合分离时，可使用三段式状态机。
- 在时序 `always` 块中，`done`、写使能、事件指示等 pulse 类信号采用“默认清零 + 条件置位”的写法，生成单个时钟周期脉冲。使用异步复位。
- 行内和分区增加中文注释，用于说明不直观的设计意图、关键时序和容易误用的接口约束，避免逐行翻译代码。
- 不为未提出的需求增加通用化结构、额外流水级、缓存、状态或异常处理。

## 2. 模块顶部注释：Module Contract

每个 Verilog 文件应在编译指令之后、`module` 声明之前使用中文块注释编写 **Module Contract（模块行为契约）**。

Module Contract 的目的不是解释 RTL 如何实现，而是让人在不阅读模块内部代码的情况下，能够快速回答：

1. 这个模块负责什么、不负责什么；
2. 一次事务如何进入、何时算完成；
3. 外部必须遵守哪些握手和时序规则；
4. reset、abort、error 等异常情况下会发生什么；
5. 哪些行为是后续 Cocotb 黑盒验证必须证明的。

Module Contract 是模块当前外部可观察行为的轻量基准，也是黑盒验证的直接输入。修改 RTL 时，如果外部可观察行为发生变化，必须在同一次修改中同步更新 Module Contract；仅内部重构、状态拆分、计数器或实现方式变化而外部行为不变时，不要求修改契约。

### 2.1 固定结构

顶部注释优先使用以下固定结构；不适用的章节可省略，不机械补齐：

```verilog
/*
 * Module Contract
 *
 * 模块职责：
 * - <一句话说明模块定位>
 * - <主要负责的功能>
 * - 不负责：<明确的重要边界>
 *
 * 输入事务：
 * - <什么事件/握手开始一次事务>
 * - <输入字段何时采样或锁存>
 * - <busy期间是否允许新事务>
 *
 * 输出事务：
 * - <产生什么输出/响应>
 * - <完成条件和完成脉冲定义>
 *
 * 关键时序：
 * - <valid/ready握手规则>
 * - <pulse宽度、RAM/FIFO固定延迟、数据保持要求等>
 * - <只有外部可观察且验证必须知道的周期关系>
 *
 * 异常与恢复：
 * - reset：<复位后的外部状态>
 * - abort/timeout：<如何取消、是否允许残留输出>
 * - error：<错误时外部可观察行为>
 *
 * 使用约束：
 * - <上游/下游必须满足的假设>
 * - <单事务、数据稳定、长度范围、参数限制等>
 */
```

### 2.2 写作要求

- **先写职责和事务，再写实现相关说明。** 顶部第一屏应优先让人看懂模块用途、事务入口和事务完成条件。
- **只写外部可观察行为和重要使用约束。** 默认不罗列 FSM 状态名、内部寄存器、计数器宽度、组合/时序拆分方式等实现细节；除非这些细节直接决定接口时序或容易导致误用。
- **关键时序必须明确。** 对 `valid/ready`、单周期 pulse、固定 RAM/FIFO 延迟、busy期间数据保持、abort 同拍优先级、完成脉冲产生条件等，不使用“适当延迟”“正常握手”之类模糊描述。
- **明确职责边界。** 对容易被误认为由本模块处理、但实际属于上下游的功能，应使用“不负责：...”直接说明。
- **避免复制其他文档。** 协议帧格式、CMD/Payload 定义等已有单一事实来源时，只引用对应文档，不在多个 RTL 顶部重复整段定义；本模块只写与自身行为直接相关的部分。
- **不把当前实现自动当作设计要求。** 如果某行为只是现有 RTL 的实现选择、并非明确要求或外部契约，不应为了描述代码而写入 Contract。
- **控制长度。** 普通模块建议约 20～50 行；复杂事务模块可适当增加，但应保持可快速浏览，不写成长篇设计文档。
- **UTF-8 编码。** 中文说明应简洁、具体，优先使用短条目而不是长段落。

### 2.3 与验证的关系

后续 Cocotb 验证应分两阶段使用 Module Contract：

1. **黑盒验证**：优先依据协议/接口规范、Module Contract 和本次修改要求建立验证声明与检查项，不先依赖 RTL 内部实现推导预期结果；
2. **白盒补充**：黑盒计划建立后再阅读 RTL，根据 FSM、分支、边界状态和实现风险补充未覆盖场景，但不得用“代码就是这样写的”替代外部行为依据。

因此，Module Contract 中每一条精确的外部行为，都应能够转化为明确的 Cocotb assertion、scoreboard、monitor、timeout 或边界测试；无法验证、无需长期保持的实现描述通常不应进入 Contract。

## 3. 行内注释

- 行内注释用于解释“为什么这样做”以及容易误解的周期关系，不重复代码字面含义。
- 状态机可在关键状态跳转处说明事务意义，但不要求为每个状态逐条解释。
- 对同步 RAM、跨模块握手、CRC 累计、方向控制等容易产生 off-by-one 或时序误判的位置，应在相关代码附近保留简短说明。
- 当代码与外部协议或项目级约束有关时，优先引用对应单一事实来源，而不是再次复制整套规则。
