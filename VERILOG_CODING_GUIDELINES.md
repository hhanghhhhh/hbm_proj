# Verilog 编码约定（Codex）

本约定只限制 RTL 的基本形式和工程风格，不规定具体实现方式。Codex 可根据模块复杂度选择合适的设计，但应保持以下风格一致。

## 编码约束

- 生成可综合、可验证、可维护的 Verilog-2001 代码；除非明确要求，不使用 SystemVerilog 专有语法。
- 每个 Verilog 文件应在编译指令之后、`module` 声明之前使用块注释用中文写明设计说明。根据模块实际情况，详细说明模块定位与职责边界、主要接口和握手语义、事务顺序、关键控制流程，以及上层使用约束；不存在的内容不机械补齐。代码行为改变时应同步更新该注释，utf-8 编码格式。
- 命名使用 `snake_case`。输入、输出端口分别使用 `i_`、`o_` 前缀；低有效信号使用 `_n` 后缀。端口名应能体现信号用途和数据流向。
- 参数、局部常量和状态常量使用大写命名；状态常量使用 `ST_` 前缀。模块例化使用具名参数和具名端口连接。
- 状态机默认使用一段式写法，在同一个时序 `always` 块中完成状态转移和寄存输出；当逻辑明显更适合分离时，可使用三段式状态机。
- 在时序 `always` 块中，`done`、写使能、事件指示等 pulse 类信号采用“默认清零 + 条件置位”的写法，生成单个时钟周期脉冲。
- 行内和分区增加中文注释用于说明接口约定、不直观的设计意图及关键时序，避免逐行解释代码。
- 不为未提出的需求增加通用化结构、额外流水级、缓存、状态或异常处理。
