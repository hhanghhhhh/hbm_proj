# Verilog 编码约定

本约定限制 RTL 的基本形式、工程风格和模块注释的写法，不规定具体实现方式。Codex 可根据模块复杂度选择合适设计，但应保持以下风格一致。

## 1. 基本编码约束

- 生成可综合、可验证、可维护的 Verilog-2001 代码；除非明确要求，不使用 SystemVerilog 专有语法。
- 命名使用 `snake_case`。输入、输出端口分别使用 `i_`、`o_` 前缀；低有效信号使用 `_n` 后缀。端口名应能体现信号用途和数据流向。时钟使用 `i_clk`，复位使用 `i_rst_n`。
- 参数、局部常量和状态常量使用大写命名；状态常量使用 `ST_` 前缀。模块例化使用具名参数和具名端口连接。
- 状态机默认使用一段式写法，在同一个时序 `always` 块中完成状态转移和寄存输出；当逻辑明显更适合分离时，可使用三段式状态机。
- 在时序 `always` 块中，`done`、写使能、事件指示等 pulse 类信号采用“默认清零 + 条件置位”的写法，生成单个时钟周期脉冲。使用异步复位。
- 行内和分区增加中文注释，用于说明不直观的设计意图、关键时序和容易误用的接口约束，避免逐行翻译代码。
- 不为未提出的需求增加通用化结构、额外流水级、缓存、状态或异常处理。

## 2. 模块顶部说明

每个 Verilog 文件应在编译指令之后、`module` 声明之前使用中文块注释编写简短的**模块说明**。

模块说明同时供人和 AI 阅读，目标是让读者在不深入 RTL 实现的情况下，快速掌握该模块最重要、最不应被误解的设计信息。

顶部说明不是完整接口规格书，也不要求把 RTL 的全部外部行为重新描述一遍。能够从结构清晰的 RTL 中直接、可靠推导出的内容通常不应重复写入顶部注释。

### 2.1 优先记录的内容

根据模块实际情况选择以下内容，不适用的内容可省略：

- **功能/职责**：模块负责什么，以及必要时说明明显的职责边界。
- **关键数据**：RAM/寄存器记录格式、位域、地址映射、编号方式、单位等。
- **业务语义**：本模块直接处理的命令、Payload 或其他关键输入数据的含义。
- **关键约束**：不能因重构而改变的设计决定、跨模块约束、数据生命周期或使用条件。
- **特殊行为**：容易误用、难以从局部 RTL 判断，或与工程通用规则不同的握手、时序、异常恢复等行为。

尤其应保留那些“代码当前能够实现，但仅看实现无法确定它是不是设计要求”的信息。例如：某种 RAM 格式必须长期兼容、某个已提交后台任务不能被 abort 取消、某种编号方式属于系统约定等。

### 2.2 默认不在顶部重复的内容

以下内容如果属于工程通用规则，或可从 RTL 中直接可靠看出，默认不写入顶部说明：

- FSM 状态名、计数器宽度、流水级等实现细节；
- 普通的 1clk pulse 行为；
- 多个模块共同采用的 `valid/ready` 或事务握手规则；
- 已统一约定的 RAM/FIFO 固定读延迟；
- 多个模块一致的 reset/abort 通用行为；
- 对代码逐句翻译得到的执行流程；
- 已有明确统一定义、且对理解本模块没有必要再次展开的协议或公共规则。

如果理解本模块确实需要某项公共规则，可简短说明或引用当前工程中的对应规范，不应复制整套定义。

判断一条顶部注释是否值得保留时，可使用以下原则：

> 如果删掉这句话后，读者或 AI 直接阅读 RTL 仍能较容易、较可靠地得到完全相同的结论，则通常不需要在顶部重复写。

换句话说：**优先写设计意图、关键数据和例外，不写代码翻译和通用惯例。**

### 2.3 推荐结构

顶部说明不要求固定章节，也不要求达到固定行数。普通模块通常应保持在十几行左右；复杂模块只在确有必要时增加内容。

推荐使用以下精简结构：

```verilog
/*
 * 模块说明
 *
 * 功能：
 * - <模块的核心职责>
 *
 * 关键数据：
 * - <RAM格式、位域、地址映射、Payload等>
 *
 * 关键约束：
 * - <需要长期保持的设计决定或跨模块约束>
 *
 * 特殊行为：
 * - <容易误用、难以直接推导或与通用规则不同的行为>
 */
```

其中 `关键数据`、`关键约束`、`特殊行为` 均可按需省略，不机械补齐。

### 2.4 顶部说明维护

修改 RTL 时，仅在顶部说明所记录的设计信息发生变化时同步修改注释：

- 模块职责、关键数据格式、重要约束或特殊行为改变时，应同步更新顶部说明；
- 仅 FSM 重构、状态拆分、计数器调整或其他内部实现变化，而上述信息未变化时，不要求修改顶部说明；
- 不应为了让顶部说明看起来完整，而补写可以直接从 RTL 可靠推导出的实现细节。

## 3. 行内注释

- 行内注释用于解释“为什么这样做”以及不直观、容易出错的局部行为，不重复代码字面含义。
- 状态机可在关键状态跳转处说明事务意义，但不要求为每个状态逐条解释。
- 对同步 RAM、跨模块握手、CRC 累计、方向控制等容易产生 off-by-one 或时序误判的位置，可在相关代码附近保留简短说明。
- 当代码与外部协议或工程级约束有关时，只说明与当前实现直接相关的部分，避免再次复制整套规则。
- 中文说明应简洁、具体，优先使用短条目而不是长段落；文件统一使用 UTF-8 编码。
