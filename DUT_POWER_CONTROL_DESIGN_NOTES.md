# 多板卡 DUT 电源控制设计说明

> 本文是 `DUT_POWER_CONTROL.md` 的说明性文档，用于记录参数选择原因和时序理解。
> 最终 RTL 约束以 `DUT_POWER_CONTROL.md` 为准。

## 1. 为什么第一版选择 250 kbit/s

故障总线只有 20 个固定节点，故障帧也只有 7 Byte，吞吐量不是主要矛盾。

第一版优先考虑：

- 20 块板并联后的总线电容；
- 开漏总线由上拉电阻产生上升沿，上升速度天然比主动推挽慢；
- 板间布线、支路、噪声和 FPGA 输入同步都会带来额外不确定性；
- 故障总线属于保护链路，宁可多花几百微秒，也应优先换取更大的采样和调试裕量。

因此第一版选择：

```text
FAULT_IO_BAUD = 250 kbit/s
bit_time      = 4 us
```

如果后续示波器确认上升/下降沿、振铃和噪声裕量都很好，再整体提高波特率即可。所有 ACK 和仲裁参数都以 `bit_time` 表示，因此提速不需要改变协议结构。

---

## 2. 为什么使用开漏共享总线

`FAULT_REQ` 和 `FAULT_IO` 都可能被多个板卡在不同时间驱动。

如果采用普通推挽输出，一旦两个 FPGA 因故障、复位或逻辑竞争同时输出相反电平，会形成直接硬件冲突。

开漏方式只允许节点：

```text
拉 Low
或
释放为 High-Z
```

High 由公共上拉电阻产生，因此多个节点同时动作时不会出现“一个推高、一个拉低”的硬冲突。

推荐结构：

```text
                     3.3 V
                       |
                    1.5 kOhm
                       |
FAULT_REQ/FAULT_IO -----+-------------------- 主干
        |               |               |
      Board0          Board1          Board19
        |               |               |
   open-drain        open-drain       open-drain
    buffer             buffer           buffer
        |               |               |
      FPGA            FPGA            FPGA
```

原则：

- 公共主干只放一处主上拉；
- 初值使用约 1.5 kOhm，并预留 1 kOhm ~ 2.2 kOhm 调整范围；
- 各板本地强上拉默认不装，避免 20 个上拉并联后等效电阻过小；
- 接收侧优先使用带施密特特性的输入缓冲；
- FPGA 最好不要直接承担跨板公共线的开漏驱动，由外部缓冲器隔离更稳妥；
- 总线布线优先主干/背板，支路尽量短。

最终应以示波器测量为准。250 kbit/s 下一个 bit 为 4 us，建议有效上升时间明显小于一个 bit，最好控制在约 1 us 量级以内；如果明显更慢，应优先调整上拉、布线或降低波特率，而不是靠 FPGA 接收逻辑硬扛。

---

## 3. 故障帧为什么保持定长

当前故障总线只需要携带：

```text
SOF | SOURCE_ID | DUT_ID | FAULT_CODE | EVENT_ID | CRC16
```

总共固定 7 Byte。

这里不增加 LENGTH、CMD 等通用通信字段，原因是：

- 故障链路功能单一；
- 定长帧更容易判断接收完成；
- ACK 起始时刻天然固定；
- 状态机更简单，异常恢复更容易验证；
- 后续如果确实出现新需求，再扩展版本，而不是第一版提前设计成通用协议。

每个 Byte 使用 UART 8N1，因此一个 7 Byte 帧占：

```text
7 Byte * 10 bit/Byte = 70 bit
70 * 4 us = 280 us
```

不需要额外的结束字节。固定长度、UART Stop Bit 和 CRC 已经足以确定帧边界。

---

## 4. CRC 和大端规则

CRC 采用 CRC-16/MODBUS 的计算参数，因为算法成熟、实现简单且现成验证工具多。

CRC 覆盖：

```text
SOURCE_ID | DUT_ID | FAULT_CODE | EVENT_ID
```

不包含 SOF。

需要特别注意：

**这里使用的是 CRC-16/MODBUS 的算法，不完全照搬 Modbus RTU 的线上字节顺序。**

本项目统一规定多字节字段采用大端，所以 CRC 在线路上发送：

```text
CRC16_H
CRC16_L
```

而不是标准 Modbus RTU 中常见的 CRC Low Byte 先发。

这样可以避免项目里同时存在两套多字节字节序规则。

---

## 5. 仲裁参数为什么这样取

当前确定：

```text
ARB_GUARD = 4 bit_time = 16 us
ARB_SLOT  = 2 bit_time = 8 us
```

`ARB_GUARD` 用来吸收：

- FAULT_REQ 从 Low 释放到 High 的 RC 上升时间；
- 不同板卡输入缓冲传播差异；
- FPGA 输入同步器带来的几个时钟差异；
- 各板本地 bit counter 启动相位差。

16 us 对这些误差来说非常宽松。

每个 Board ID 相差 8 us 的仲裁优先级：

```text
Board0  : 16 us
Board1  : 24 us
Board2  : 32 us
...
Board19 : 168 us
```

到达自身时刻后还必须重新检查 `FAULT_REQ`。如果更高优先级节点已经拉 Low，本板立即放弃当前仲裁并清零计数。

### 多板同时 Fault 示例

```text
FAULT_REQ  ────────────────────────────────\____________________

Board3 fault      ^
Board8 fault      ^
                  |
                  +---- 两块板同时开始等待

ARB_GUARD         |<----16 us---->|

Board3 target                       ^
                                   |
                                   +---- Board3 拉低 FAULT_REQ，取得总线

Board8 target                              ^
                                           |
                                           +---- 到时前已看到 REQ Low，放弃仲裁
```

当前事务结束后：

```text
FAULT_REQ  ________________________/-----------------------------
                                      |
                                      +-- Board8 pending 仍在
                                          重新从 ARB_GUARD 开始仲裁
```

因此同一板卡多个 DUT fault 也采用相同原则：一次只发一个，结束后重新参与下一轮仲裁。

---

## 6. ACK 参数为什么这样取

当前确定：

```text
TURNAROUND_GAP = 4 bit_time = 16 us
ACK_SLOT       = 6 bit_time = 24 us
```

一个 ACK slot：

```text
|<------ 24 us ------>|

| 8 us | 8 us | 8 us |
|guard | ACK  |guard |
         Low
```

接收节点只有完整收到合法故障帧并完成本地 DUT KILL 后，才允许在自己的 ACK slot 中将 `FAULT_IO` 拉 Low。

两侧各留 2 bit guard，主要是为了避免：

- 相邻节点时隙边界误差；
- 开漏线上升沿较慢；
- 不同 FPGA 本地波特率误差；
- 状态从接收切换到 ACK 驱动时过于紧张。

20 个节点总 ACK 窗口为：

```text
20 * 24 us = 480 us
```

虽然时间不短，但换来的协议裕量很大，第一版值得这么做。

---

## 7. 一次完整事务的大概时间

### 7.1 已取得总线后的正常事务

```text
FAULT_REQ Low
    |
    |<------ Fault Frame ------>| 280 us
    |
    |<-- Turnaround -->|          16 us
    |
    |<--------------- ACK[0..19] --------------->| 480 us
    |
FAULT_REQ High
```

总计：

```text
280 + 16 + 480 = 776 us
```

### 7.2 包含最坏首次仲裁

Board19 首次取得总线的最坏仲裁等待约：

```text
ARB_GUARD + 19 * ARB_SLOT
= 16 + 19 * 8
= 168 us
```

因此健康总线下，从进入仲裁到 Board19 完成一次跨板故障通知约：

```text
168 + 776 = 944 us
```

也就是说，即使采用非常保守的 250 kbit/s，第一次正常故障传播仍大致保持在 1 ms 级。

### 7.3 重试

重试期间发起方继续保持 `FAULT_REQ = Low`，不重新参加仲裁。

每多一次重试，大约再增加：

```text
776 us
```

`MAX_RETRY = 2` 表示：

```text
首次发送 + 最多 2 次重发 = 最多 3 次发送
```

重试路径允许超过 1 ms，因为这已经属于通信异常场景。相比硬性卡住 1 ms，更重要的是尽可能把 KILL 信息送达到所有还能正常工作的板卡，并在最终失败后释放 Fault Bus，让其他 DUT 的新故障仍有机会传播。

---

## 8. FAULT_IO 整体时序示意

```text
FAULT_REQ
High ────────────────\________________________________________________/──── High
                     ^                                                ^
                     |                                                |
                  仲裁成功                                          事务结束

FAULT_IO
High ────────────────┐
                     │  UART 8N1 Fault Frame, 7 Byte
                     └─[SOF][SRC][DUT][CODE][EVENT][CRC_H][CRC_L]─┐
                                                                  │
                     <------------- 16 us GAP -------------------->
                                                                  │
       ACK0        ACK1        ACK2                       ACK19    │
      24 us       24 us       24 us                      24 us     │
    |--------|   |--------|   |--------|       ...      |--------| │
High__\____/__...__\____/__...___________________________\____/____┘
       Low=ACK
```

ACK 不要求每个 slot 都出现 Low；未出现 Low 的 slot 即对应 missing ACK。

---

## 9. START_SYNC 为什么独立于故障总线

正常上/下电需要的是统一时间起点，而不是节点之间传递大量实时命令。

因此正常流程采用：

```text
PC 逐板配置 / ARM / READY
              |
              v
      所有相关板卡准备完成
              |
              v
        START_SYNC 有效沿
              |
       +------+------+------+
       v      v      v      v
     Board0 Board1  ...   Board19
       本地 sequence 从 T=0 开始
```

这样 RS485 的不确定发送延迟不会进入真实电源时序。

故障总线则只解决异步的 DUT KILL 传播，两者职责独立，便于验证和排查。

---

## 10. 第一版调试时重点观察

协议参数先不要急着提速，联调时优先用示波器/逻辑分析仪观察：

- 20 块板全部接入后 `FAULT_REQ` / `FAULT_IO` 的上升时间；
- 开漏释放后的波形是否有长时间停留在输入阈值附近；
- 主干和最远板卡的振铃、过冲、地弹噪声；
- 相邻 ARB slot 是否有足够间隔；
- ACK slot 的 Low 脉冲是否稳定落在各自窗口中央；
- 最远节点与最近节点的采样是否一致；
- 多板同时 fault、故障发生在 BUSY 期间、ACK 丢失和重试等异常场景。

只有这些都稳定后，再考虑从 250 kbit/s 提高到 500 kbit/s 或 1 Mbit/s。
