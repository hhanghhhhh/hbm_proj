# FPGA 通信架构设计说明

## 1. 目的

本文定义 FPGA 内部通信处理模块的总体架构。

设计目标：

- 通信协议解析与业务功能解耦；
- 支持参数/状态读取、设备控制、大块配置传输、在线升级等功能；
- 通信数据经过完整校验后再进入业务处理；
- 便于后续扩展 CMD 和维护。

---

## 2. 总体架构

FPGA 通信模块采用分层设计：

```
UART RX/TX
    |
    v
Frame Parser
    |
    v
CMD Dispatcher
    |
    +----------------+
    |                |
    v                v
参数/状态模块      配置模块
    |
    +----------------+
    |                |
    v                v
控制模块        在线升级模块

业务响应
    |
    v
Response Manager
    |
    v
TX Frame Builder
    |
    v
UART TX
```

---

## 3. Frame Parser

职责：

负责基础通信帧解析。

处理内容：

- SOF 检测；
- ADDR、CMD、SEQ、LENGTH 字段解析；
- PAYLOAD 接收；
- CRC 计算与校验；
- 长度检查；
- 接收异常处理。

输出：

- 合法完整的数据帧；
- CMD；
- SEQ；
- LENGTH；
- PAYLOAD 数据访问接口。

约束：

- 不解析业务含义；
- 不处理具体 CMD；
- CRC 校验通过后才提交给业务模块。

---

## 4. CMD Dispatcher

职责：

根据 CMD 将请求分发到对应业务模块。

功能分类：

- 参数 / 状态类；
- 控制类；
- 配置数据类；
- 在线升级类；
- 批量读取类。

约束：

- 只负责命令路由；
- 不执行具体业务逻辑。

---

## 5. 业务模块

### 5.1 参数 / 状态模块

负责：

- 设备信息读取；
- 当前状态读取；
- 实时采样值读取；
- 参数读取。

特点：

- 小数据量；
- 请求-响应模式。

---

### 5.2 控制模块

负责设备控制命令：

- START_OUTPUT；
- STOP_OUTPUT；
- RESET；
- CLEAR_FAULT。

执行前需要检查设备状态，并返回执行结果。

---

### 5.3 配置数据模块

负责接收设备配置数据。

流程：

```
接收配置数据
      |
      v
配置缓存 RAM
      |
      v
校验
      |
      v
配置执行
```

设计原则：

- 通信模块只负责数据搬运；
- 不解析配置内容；
- 配置数据按照地址写入配置 RAM；
- 配置执行模块负责解析和应用。

通信层只关心：

```
offset
 data
```

不关心具体配置字段定义。

---

### 5.4 在线升级模块

负责固件升级流程。

流程：

```
升级开始
   |
数据分块传输
   |
完整性校验
   |
写入存储
   |
激活升级
```

特点：

- 大数据传输；
- 支持分包；
- 完整校验后生效。

---

### 5.5 批量读取模块

用于高效读取多个状态或参数。

设计原则：

- 一次请求返回多个相关数据；
- 保证返回数据来自同一时间快照；
- 减少通信次数。

---

## 6. 配置数据处理原则

配置数据采用：

```
接收
 |
缓存
 |
校验
 |
提交
 |
执行
```

禁止：

- 未完整接收时直接修改有效配置；
- CRC 未通过时执行关键操作。

建议使用临时配置区和有效配置区分离：

```
STAGING_CONFIG
        |
      COMMIT
        |
        v
ACTIVE_CONFIG
```

---

## 7. 响应处理架构

业务模块不直接生成完整通信帧。

业务模块只产生：

- 响应 CMD；
- SEQ；
- STATUS；
- PAYLOAD 数据。

统一由 TX Frame Builder 完成：

- SOF 添加；
- ADDR 添加；
- LENGTH 计算；
- CRC 计算。

---

## 8. 设计原则总结

1. 通信层与业务层分离。
2. Frame Parser 只负责可靠收帧。
3. CMD Dispatcher 只负责命令分发。
4. 业务模块不关心 UART 帧格式。
5. CRC 校验通过后再执行关键操作。
6. 大数据采用分块传输。
7. 配置数据采用缓存后提交机制。
8. 响应统一由 TX Frame Builder 生成。
9. 新功能优先通过增加 CMD 和业务模块实现，不修改基础通信框架。
