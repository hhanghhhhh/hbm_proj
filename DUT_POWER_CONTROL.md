# 多板卡 DUT 电源控制架构

## 1. 目的

本文定义多板卡场景下 DUT 电源的同步上电、正常下电和故障联动关断框架。

系统当前约束：

- 共 20 个板卡挂在同一条 RS485 总线上；
- 每个板卡控制 128 路 EN，每一路 EN 视为一路电源；
- 一个 DUT 的多路电源可能分散在多个板卡；
- DUT 上电、下电存在时序要求，同步精度约为 1 ms；
- Master 为 PC，不参与实时故障保护闭环；
- 板卡之间除 RS485 外，预留若干可配置硬件信号；
- 当前规划使用 `START_SYNC`、`FAULT_REQ`、`FAULT_IO` 三根公共硬件线。

设计目标：

- 正常上/下电时，各板卡按同一时间基准执行本地时序；
- 任意一路电源故障时，只关闭该 DUT 的全部电源，不影响其他 DUT；
- 故障联动不依赖 PC 的实时响应；
- 对跨板故障关断提供接收确认和重试能力，不能假定一次广播必然成功。

---

## 2. 基本对象和本地配置

系统控制的一级逻辑对象为 DUT，而不是单独的板卡或 EN。

每个板卡保存本板的 DUT 映射信息：

```text
DUT_ID -> EN_MASK[127:0]
```

`EN_MASK` 表示该 DUT 在当前板卡占用的全部 EN。

同一个 DUT 可以同时存在于多个板卡，每个板卡只需要知道该 DUT 在本板上的 EN 映射，不需要知道其他板卡的具体接线。

板卡还需要保存 DUT 的本地电源时序配置，至少区分：

- 上电时序；
- 正常下电时序；
- 故障关断行为。

正常上/下电由本地时序执行模块完成；故障关断优先级高于正常时序。

---

## 3. 三根公共硬件线

### 3.1 START_SYNC

`START_SYNC` 用于多个板卡之间的统一动作起点。

正常上电或正常下电前，PC 通过 RS485 将目标 DUT、动作类型及所需时序配置准备到相关板卡，并逐板确认 READY。

所有相关板卡准备完成后，由指定板卡产生 `START_SYNC` 有效沿。所有板卡以该事件作为本地时序的统一 `T=0`，之后各自按照已经保存的时序执行。

因此：

- RS485 负责配置和准备确认；
- `START_SYNC` 负责实时同步；
- 不依赖 PC 按顺序逐板发送实时 EN 控制命令。

### 3.2 FAULT_REQ

`FAULT_REQ` 用于故障总线仲裁和总线占用指示。

定义：

- `FAULT_REQ = High`：故障总线空闲；
- `FAULT_REQ = Low`：已有板卡取得故障总线所有权，当前存在正在处理的故障事务。

发生故障的板卡不能立即拉低 `FAULT_REQ`，而是先进入固定时隙仲裁。只有仲裁成功的板卡才能拉低 `FAULT_REQ` 并成为本次故障事务发起方。

事务发起方从取得总线所有权开始持续保持 `FAULT_REQ` 有效，直到本次故障帧发送、ACK 和必要重试结束后释放。

`FAULT_REQ` 只表示故障总线是否被占用，不表示系统故障状态是否已经清除。即使存在节点长期未 ACK，也不得永久占用 `FAULT_REQ`，达到重试上限后必须结束本次事务并释放总线。

建议采用所有板卡均可主动拉低、空闲时释放的共享方式。具体电气实现需结合板卡接口电路最终确认。

### 3.3 FAULT_IO

`FAULT_IO` 是板卡间专用的小数据量故障通信线。

用途：

- 发送故障事件；
- 携带 `DUT_ID`、故障来源、故障类型等必要信息；
- 在故障帧发送完成后复用为各板卡 ACK 通道。

`FAULT_IO` 不承担普通配置或状态通信，不替代 RS485。

建议采用空闲为高、节点只主动拉低的共享总线形式，避免多个 FPGA 同时驱动不同电平产生硬件冲突。具体电气实现需结合板卡接口电路最终确认。

---

## 4. 故障处理流程

任意板卡检测到故障后，本板关断和跨板通知是两个独立动作。

本板关断不得等待通信完成：

```text
Local Fault
    |
    +--> DUT_ID = lookup(fault EN)
    |
    +--> local EN <= EN & ~DUT_MASK[DUT_ID]
    |
    +--> record pending fault
```

之后由故障总线仲裁机制决定何时发送该故障事件。

其他板卡收到合法故障帧后，根据自己的 `DUT_MASK[DUT_ID]` 立即关闭本板属于该 DUT 的全部 EN。

即使某个板卡对应 DUT 的 mask 为 0，也仍应完成该故障事务的接收和 ACK。

故障关断命令必须设计为幂等操作：同一故障事件被重复接收或重发，不得产生额外副作用。

一个板卡同时存在多个待发送 DUT 故障时，应分别记录 pending 状态。本地关断立即执行，跨板故障通知按事务逐个发送，不要求一个故障帧同时携带多个 DUT。

---

## 5. FAULT_IO 数据格式

当前方案采用 UART 风格的异步串行格式，所有板卡使用相同波特率。

单字节格式暂定为：

```text
1 Start + 8 Data + No Parity + 1 Stop
```

即 8N1。

接收端使用起始位重新建立字节采样基准，不要求各板卡共享同一系统时钟。

当前故障帧采用固定长度，不设置 LENGTH 字段，暂定如下：

| Byte | 字段 | 含义 |
|---|---|---|
| 0 | SOF | 固定帧头，暂定 `0xA5` |
| 1 | SOURCE_ID | 故障发起板卡编号 |
| 2 | DUT_ID | 需要故障关断的 DUT |
| 3 | FAULT_CODE | 故障类型 |
| 4 | EVENT_ID | 故障事件编号，用于识别同一次故障的重发 |
| 5 | CRC16_H | CRC16 高字节 |
| 6 | CRC16_L | CRC16 低字节 |

`EVENT_ID` 在同一次故障事务重发时保持不变。

CRC16 的具体算法参数当前未确定。

---

## 6. ACK 机制

`FAULT_IO` 不采用所有节点同时 ACK 的方式，而是使用固定板卡编号对应固定 ACK 时隙。

故障帧最后一个 Stop Bit 结束后：

```text
Fault Frame
    |
    +--> TURNAROUND_GAP
    |
    +--> ACK_SLOT[0]
    +--> ACK_SLOT[1]
    +--> ...
    +--> ACK_SLOT[19]
```

当前时序参数暂定：

```text
TURNAROUND_GAP = 2 bit_time
ACK_SLOT        = 4 bit_time
```

每个 ACK slot 内建议采用：

```text
1 bit_time  guard
2 bit_time  ACK active
1 bit_time  guard
```

ACK 在线路上的有效状态为拉低 `FAULT_IO`。

每块板根据自己的 `BOARD_ID` 只在对应 slot 内响应。故障发起方按照同一 bit_time 计数窗口逐个检测 ACK slot，并形成：

```text
ack_bitmap[19:0]
```

ACK 表示：

- 故障帧格式正确；
- SOF、CRC、DUT_ID 等检查通过；
- 本板已经完成该 DUT 的故障关断处理。

故障发起板本身不要求在线路上对自己 ACK，可直接将自身对应 `ack_bitmap` 位置为成功。

如果存在未 ACK 节点，发起方自动重发同一 `EVENT_ID` 的故障帧。达到最大重试次数后结束本次事务、记录未 ACK 节点并释放 `FAULT_REQ`，不能因为单个节点异常长期阻塞后续故障事务。

最大重试次数及异常状态的具体上报方式待后续确定。

---

## 7. 多板卡故障仲裁

多个板卡可能同时或近同时产生故障。仲裁采用基于 `BOARD_ID` 的固定等待时隙，不增加额外仲裁数据帧。

基本规则：

1. 板卡产生故障后立即完成本地 DUT 关断，并记录 pending fault；
2. 当 `FAULT_REQ = Low` 时，所有板卡的仲裁等待计数保持清零，不参与当前事务竞争；
3. 当 `FAULT_REQ = High` 且本板存在 pending fault 时，从头开始仲裁等待；
4. 各板卡按照固定的 `BOARD_ID` 对应不同等待时隙；
5. 到达本板仲裁时刻时必须再次确认 `FAULT_REQ` 仍为 High，确认空闲后才能拉低 `FAULT_REQ` 并取得总线所有权；
6. 任意板卡检测到 `FAULT_REQ` 被其他板卡拉低后，立即清零自己的仲裁等待计数，保留 pending fault，等待当前事务结束；
7. 当前事务结束、`FAULT_REQ` 重新变 High 后，仍有 pending fault 的板卡重新从头参与仲裁。

仲裁优先级由 `BOARD_ID` 固定决定，不保证故障事件严格按照产生时间先后顺序发送。当前系统故障事务数据量小、事务时间短，不额外引入公平仲裁机制。

为避免 `FAULT_REQ` 释放沿在不同 FPGA 上的同步误差直接影响最低编号节点，仲裁窗口建议保留统一的起始保护时间：

```text
ARB_GUARD + BOARD_ID * ARB_SLOT
```

`ARB_GUARD` 和 `ARB_SLOT` 的具体长度后续结合最终波特率统一确定，并继续使用 `bit_time` 作为时间单位。

同一板卡存在多个 pending fault 时，每次取得总线只发送一个故障事务；事务结束后如仍有 pending fault，则重新参与下一轮仲裁。

---

## 8. 时间基准

`FAULT_IO` 和故障仲裁的协议时序统一使用 `bit_time` 表示，包括：

- UART 字节发送和接收；
- TURNAROUND_GAP；
- ACK slot；
- ACK 有效宽度；
- ARB_GUARD；
- ARB_SLOT。

不在协议定义中混用固定微秒延时和波特率计数。

当前讨论以 1 Mbps 作为评估值：

```text
1 bit_time = 1 us
```

按 7 字节 8N1 故障帧、2 bit turnaround、20 个 4 bit ACK slot 估算，一次完整故障事务约 152 us，因此在 1 ms 级故障同步目标下有足够的重试余量。

最终波特率仍需结合 PCB 信号质量、线路长度和接口电路确认。

---

## 9. 与 RS485 的职责划分

RS485 保持现有 PC Master、板卡 Slave 的通信架构，主要负责：

- DUT/EN 映射配置；
- 上下电时序配置；
- ARM / READY 等正常控制准备；
- 状态读取；
- 故障记录读取；
- 其他普通业务通信。

对必须确保所有板卡正确执行的实时动作，不依赖“发送一次 RS485 广播即认为成功”。

正常同步动作采用：

```text
RS485 逐板准备和确认
        +
START_SYNC 统一触发
```

故障同步动作采用：

```text
FAULT_REQ 仲裁 / 总线占用
        +
FAULT_IO 故障帧
        +
固定时隙 ACK / 自动重试
```

PC 不参与故障关断的实时闭环，只负责后续状态读取、记录和人工处理。

---

## 10. 优先级和安全约束

- 本板检测到故障后，必须先执行本地 DUT 关断，不得等待跨板通信；
- 跨板收到故障帧后，故障关断优先于正在执行的正常上电/下电时序；
- 故障状态下不得因为后续 `START_SYNC` 或普通控制命令重新打开该 DUT，恢复条件需由独立的故障清除流程控制；
- 同一个 DUT 的故障消息允许重复接收；
- 单个 DUT 故障不得影响其他 DUT 的 EN；
- 通信接收错误时不得错误关闭其他 DUT；只有完整合法的故障帧才能触发跨板 DUT 关断；
- `FAULT_REQ` 不得因 ACK 异常被永久占用；
- 未取得 `FAULT_REQ` 所有权的板卡不得驱动故障帧发送阶段的 `FAULT_IO`。

---

## 11. 待确认事项

当前框架已经确定三线职责、DUT mask、故障帧、固定时隙 ACK 和基于 Board ID 的故障总线仲裁方向，以下细节仍需后续讨论后固定：

- `START_SYNC` 的具体产生方及一次触发对应的动作上下文管理方式；
- `FAULT_IO` 最终波特率；
- SOF、FAULT_CODE、EVENT_ID 的具体编码；
- CRC16 算法参数；
- ACK 最大重试次数及失败后的状态上报方式；
- `ARB_GUARD`、`ARB_SLOT` 的最终长度；
- 同一板卡多个 pending fault 的发送顺序；
- `FAULT_REQ`、`FAULT_IO` 的最终电气连接方式及上拉参数。

在上述事项确定前，RTL 不应自行扩展复杂协议机制。
