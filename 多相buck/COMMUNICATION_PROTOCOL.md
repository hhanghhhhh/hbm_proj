# FPGA 通信协议规范

## 1. 帧格式

所有命令请求和响应均采用统一帧格式。

| 字段 | 长度 |
|---|---:|
| SOF | 2 Byte |
| ADDR | 1 Byte |
| CMD | 1 Byte |
| SEQ | 1 Byte |
| LENGTH | 2 Byte |
| PAYLOAD | N Byte |
| CRC | 2 Byte |

```text
SOF | ADDR | CMD | SEQ | LENGTH | PAYLOAD | CRC
```

## 2. 基本规则

- SOF 固定为 `0x55AA`。
- ADDR 用于区分目标设备。
- 请求和响应使用相同帧格式。
- LENGTH 表示 PAYLOAD 长度，最大支持 2048 Byte。
- 多字节数据统一采用大端格式。
- CRC 校验范围不包含 SOF。

## 3. 请求响应模式

当前协议采用主机发起的严格一问一答模式：

- 主机每次只发送一个请求；
- 设备完成该请求的业务处理并返回完整响应；
- 主机收到完整响应后，才发送下一请求；
- 当前版本不支持多请求流水、并发事务或多响应排队。

## 4. 响应上下文规则

正常响应和错误响应统一沿用当前请求的元信息：

```text
响应 ADDR = 请求 ADDR
响应 CMD  = 请求 CMD
响应 SEQ  = 请求 SEQ
```

SEQ 当前仅用于请求/响应匹配，不提供自动去重、历史响应缓存等功能。

## 5. Response Payload 规则

所有响应 Payload 统一格式：

```text
+--------+----------------+
| STATUS | DATA           |
+--------+----------------+
| 1 Byte | N Byte         |
+--------+----------------+
```

规则：

- Response Payload 第一个字节固定为 STATUS；
- 正常响应 STATUS = 0 表示成功；
- 非零 STATUS 表示错误，后续字节可携带错误信息；
- 所有响应 LENGTH 至少为 1。

## 6. 错误处理原则

- SOF 同步失败、接收超时、LENGTH 非法、CRC 错误等导致当前帧本身不可信的情况直接丢弃，不生成响应；
- 未定义 CMD、参数非法、状态不允许执行等完整合法帧但请求无法执行的情况，生成统一错误响应。

## 7. 设计原则

- 接收和发送过程中均采用流式 CRC 处理；
- CRC 校验通过后，才允许执行配置更新或关键控制命令；
- 大段数据采用分包传输方式。

CRC 的具体算法参数后续统一确定。
