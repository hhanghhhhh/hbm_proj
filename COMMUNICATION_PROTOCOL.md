# FPGA 通信协议规范

## 1. 帧格式

所有命令请求和响应均采用统一帧格式。

| 字段 | 长度 |
|---|---:|
| SOF | 2 Byte |
| ADDR | 1 Byte |
| CMD | 1 Byte |
| SEQ | 1 Byte |
| LENGTH | 2 Byte |
| PAYLOAD | N Byte |
| CRC | 2 Byte |

帧格式：

```text
+--------+---------+--------+--------+--------+---------+---------+
| SOF    | ADDR    | CMD    | SEQ    | LENGTH | PAYLOAD | CRC     |
+--------+---------+--------+--------+--------+---------+---------+
| 2 Byte | 1 Byte  | 1 Byte | 1 Byte | 2 Byte | N Byte  | 2 Byte  |
+--------+---------+--------+--------+--------+---------+---------+
```

## 2. 基本规则

- SOF 固定为 `0x55AA`。
- ADDR 用于区分目标设备。
- 请求和响应使用相同帧格式。
- LENGTH 表示 PAYLOAD 长度，最大支持 2048 Byte。
- 多字节数据统一采用大端格式。
- CRC 校验范围不包含 SOF。

## 3. 请求响应模式

当前协议采用主机发起的严格一问一答模式：

- 主机每次只发送一个请求；
- 设备完成该请求的业务处理并返回完整响应；
- 主机收到完整响应后，才发送下一请求；
- 设备在当前响应发送完成前，不接受下一条业务请求；
- 当前版本不支持多请求流水、并发事务或多响应排队。

## 4. 设计原则

- 接收过程中支持边接收边计算 CRC。
- CRC 校验通过后，才允许执行配置更新或关键控制命令。
- 大段数据采用分包传输方式。
- 配置更新和固件升级需要支持完整校验后再提交生效。
