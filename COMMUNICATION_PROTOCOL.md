# FPGA 通信协议规范

## 1. 帧格式

所有命令请求和响应均采用统一帧格式。

| 字段 | 长度 |
|---|---:|
| SOF | 2 Byte |
| ADDR | 1 Byte |
| CMD | 1 Byte |
| SEQ | 1 Byte |
| LENGTH | 2 Byte |
| PAYLOAD | N Byte |
| CRC | 2 Byte |

帧格式：

```text
+--------+---------+--------+--------+--------+---------+---------+
| SOF    | ADDR    | CMD    | SEQ    | LENGTH | PAYLOAD | CRC     |
+--------+---------+--------+--------+--------+---------+---------+
| 2 Byte | 1 Byte  | 1 Byte | 1 Byte | 2 Byte | N Byte  | 2 Byte  |
+--------+---------+--------+--------+--------+---------+---------+
```

## 2. 基本规则

- SOF 固定为 `0x55AA`。
- ADDR 用于区分目标设备。
- 请求和响应使用相同帧格式。
- LENGTH 表示 PAYLOAD 长度，最大支持 2048 Byte。
- 多字节数据统一采用大端格式。
- CRC 校验范围不包含 SOF。

## 3. 请求响应模式

当前协议采用主机发起的严格一问一答模式：

- 主机每次只发送一个请求；
- 设备完成该请求的业务处理并返回完整响应；
- 主机收到完整响应后，才发送下一请求；
- 设备在当前响应发送完成前，不接受下一条业务请求；
- 当前版本不支持多请求流水、并发事务或多响应排队。

## 4. 响应上下文规则

正常响应和错误响应统一沿用当前请求的元信息：

```text
响应 ADDR = 请求 ADDR
响应 CMD  = 请求 CMD
响应 SEQ  = 请求 SEQ
```

业务模块不自行修改这些字段。

## 5. 错误处理原则

错误分层处理：

- SOF 同步失败、接收超时、LENGTH 非法、CRC 错误等导致当前帧本身不可信的情况，由 `Frame Parser` 直接丢弃，不生成响应；
- 未定义 CMD、参数非法、状态不允许执行等“完整合法帧但请求无法执行”的情况，生成统一错误响应；
- 错误响应与正常响应使用相同帧格式和发送链路，错误类型由 Payload 中的错误码表达。

详细约束见 `ERROR_RESPONSE.md`。

## 6. 设计原则

- 接收和发送过程中均采用流式 CRC 处理，不对 Payload 进行二次扫描；
- CRC 校验通过后，才允许执行配置更新或关键控制命令；
- 大段数据采用分包传输方式；
- 配置更新和固件升级需要支持完整校验后再提交生效。

CRC 的具体算法参数后续统一确定。
